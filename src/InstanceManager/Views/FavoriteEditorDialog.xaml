<Window x:Class="InstanceManager.Views.FavoriteEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Instance Manager" SizeToContent="Height" Width="440"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize"
        Background="{DynamicResource Brush.Window}"
        TextElement.Foreground="{DynamicResource Brush.TextPrimary}"
        TextElement.FontFamily="Segoe UI" TextElement.FontSize="13">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="38" ResizeBorderThickness="0" CornerRadius="0" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1">
        <StackPanel Margin="0">
            <TextBlock x:Name="TitleText" Text="Edit favorite" FontWeight="SemiBold" Margin="18,16,18,12" />

            <TextBlock Text="Name" FontSize="11" Foreground="{DynamicResource Brush.TextSecondary}" Margin="18,0,18,4" />
            <TextBox x:Name="NameBox" Style="{StaticResource ModernTextBox}" Margin="18,0,18,10" KeyDown="Box_KeyDown" />

            <TextBlock Text="Game link or PlaceId" FontSize="11" Foreground="{DynamicResource Brush.TextSecondary}" Margin="18,0,18,4" />
            <TextBox x:Name="TargetBox" Style="{StaticResource ModernTextBox}" Margin="18,0,18,10" KeyDown="Box_KeyDown" />

            <TextBlock Text="Job ID (optional)" FontSize="11" Foreground="{DynamicResource Brush.TextSecondary}" Margin="18,0,18,4" />
            <TextBox x:Name="JobBox" Style="{StaticResource ModernTextBox}" Margin="18,0,18,4" KeyDown="Box_KeyDown" />

            <TextBlock x:Name="ErrorText" Foreground="{DynamicResource Brush.Danger}" FontSize="11"
                       Margin="18,6,18,0" TextWrapping="Wrap" Visibility="Collapsed" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="18,14,18,16">
                <Button Style="{StaticResource GhostButton}" Content="Cancel" Click="Cancel_Click" Margin="0,0,8,0" />
                <Button Style="{StaticResource PrimaryButton}" Content="Save" Click="Ok_Click" IsDefault="True" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
