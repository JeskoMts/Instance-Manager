<Window x:Class="InstanceManager.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Instance Manager" SizeToContent="Height" Width="430"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize"
        Background="{DynamicResource Brush.Window}"
        TextElement.Foreground="{DynamicResource Brush.TextPrimary}"
        TextElement.FontFamily="Segoe UI" TextElement.FontSize="13">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="0" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{DynamicResource Brush.BorderStrong}" BorderThickness="1" CornerRadius="10"
            Background="{DynamicResource Brush.Surface}">
        <Grid Margin="20">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <Border Width="34" Height="34" CornerRadius="17" Background="{DynamicResource Brush.Elevated}" Margin="0,0,12,0">
                    <TextBlock Style="{StaticResource Glyph}" Text="&#xE7BA;" Foreground="{DynamicResource Brush.Danger}"
                               HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" />
                </Border>
                <TextBlock x:Name="TitleText" Text="Confirm action" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>
            <TextBlock x:Name="MessageText" Grid.Row="1" Margin="46,10,0,0" TextWrapping="Wrap"
                       Foreground="{DynamicResource Brush.TextSecondary}" />
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Style="{StaticResource GhostButton}" Content="Cancel" IsCancel="True" Click="Cancel_Click" Margin="0,0,8,0" />
                <Button x:Name="ConfirmButton" Style="{StaticResource PrimaryButton}" Content="Delete" IsDefault="True" Click="Confirm_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
